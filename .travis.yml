dist: focal
language: php
php:
  - 7.3
  - 7.4
services:
  - postgresql
cache:
  directories:
    - vendor
before_script:
#  - cp .env.travis .env
#  - sudo mysql -e 'create database homestead;'
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - php artisan key:generate
  - php artisan migrate
script:
  - vendor/bin/phpunit
after_success:
  - curl -T * ftp://domain-api-php:PqaKcT.shCxB8Vh@files.000webhost.com/
